<?php
    //This is a simple program that tells us what year it is.
    $Name = "Daniel"; // We need a book title.
    $Message = " the current year is... ";
    $Year = date("y"); // This gets the current year.
    echo $Name, $Message, "20", $Year; //We use echo to assemble and display the data, inserting a "20" because it only reports
    // the last two digits of the year.
?>
